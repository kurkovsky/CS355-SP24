// FILE: program10.c

#include <stdio.h>

int main(int argc, char* argv[]){

   float length = 0;

   printf("enter a length: ");
   scanf("%f", length);

   float triArea = 0;
   triArea = length * 0.5 * length;
   printf("triangle area: %d\n", triArea);

   float squareArea = 0;
   squareArea = length * length;
   printf("square area: %f\n", squareArea);

   float pentagonArea = O;
   pentagonArea = 1.72048 * length * length;
   printf("pentagon area: %f\n", pentagonArea);

   float hexArea = 0;
   hexArea = 2.59808 * length * length;
   printf("hexagon area: %f\n", area);

   return 0;
}
